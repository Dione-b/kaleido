name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Dist tag: alpha, beta, next, or latest"
        required: true
        default: "next"
  release:
    types:
      - published

permissions:
  contents: read
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm test

      - name: Pack packages
        run: pnpm pack:packages

      - name: Consumer isolation
        env:
          SKIP_PACK: "1"
          PACKED_DIR: ${{ github.workspace }}/packed
        run: pnpm test:consumer

      - name: Client bundler consumers
        env:
          PACKED_DIR: ${{ github.workspace }}/packed
        run: pnpm test:consumer:client-bundlers

      - name: Publish
        if: github.event_name == 'release'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        # Dist-tag: "next" for pre-v1; switch to "latest" when appropriate.
        run: pnpm publish -r --access public --provenance --tag next
